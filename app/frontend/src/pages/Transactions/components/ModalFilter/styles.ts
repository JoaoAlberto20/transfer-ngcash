import styled from 'styled-components'

export const ContainerFormFilter = styled.form`
  margin-top: 1.5rem;

  button {
    margin-top: 1.5rem;
  }
`

export const ContainerRadiosButton = styled.div`
  margin: 1rem 0;
  > div {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 1rem;
  }
`

export const ContainerFilter = styled.div`
  select,
  input {
    margin: 1rem 0;
  }
`
